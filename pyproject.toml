[project]
name = "codegen2"
version = "0.1.0"
description = "CLI coding agent that understands natural language and interacts with your codebase"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.13.5",
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
    "requests==2.32.3",
]